name: Generate Snake Animation

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: platane/snake@v3
        with:
          github_user_name: ifung-0
          outputs: |
            github-contribution-grid-snake-dark.svg?color=dark
